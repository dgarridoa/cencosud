version: '3.9'

services:
  app:
    build:
      context: .
    ports:
      - 8000:5000
    volumes:
      - ./:/app
    command: pytest 
  mysqldb:
    image: mysql:8.0.29
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: secret
    volumes:
      - mysql:/var/lib/mysql
      - mysql_config:/etc/mysql

volumes:
  mysql:
  mysql_config:
